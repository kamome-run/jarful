# Contributing

Issues and pull requests are welcome, in Japanese or English.

1. Fork and create a branch from `main`.
2. Keep the behaviour aligned with `docs/SPEC.md`; propose spec changes in the same PR.
3. Run `./gradlew :shared:desktopTest` before pushing.
4. Open a pull request. CI builds the Android APK and the Windows installer.
